import { FC } from "react";

type Props = {
  setMatchaNotif: React.Dispatch<React.SetStateAction<string | null>>;
};

const DisplayProfileUser: FC<Props> = ({ setMatchaNotif }) => {
  return <></>;
};

export default DisplayProfileUser