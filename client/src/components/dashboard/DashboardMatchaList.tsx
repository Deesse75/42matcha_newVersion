import { FC } from 'react';

type Props = {
  setMatchaNotif: React.Dispatch<React.SetStateAction<string | null>>;
};

const DashboardMatchaList: FC<Props> = ({}) => {
  return <></>;
};

export default DashboardMatchaList;
