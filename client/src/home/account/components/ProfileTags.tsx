import { FC } from "react";

type Props = {
  setMatchaNotif: React.Dispatch<React.SetStateAction<string | null>>;
};

const ProfileTags: FC<Props> = ({}) => {
  return <div>ProfileTags</div>;
};

export default ProfileTags;
