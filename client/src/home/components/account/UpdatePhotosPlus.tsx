import { FC } from 'react';

type Props = {
  setMatchaNotif: React.Dispatch<React.SetStateAction<string | null>>;
};

const UpdatePhotosPlus: FC<Props> = ({  }) => {
  return <div>UpdatePhotosPlus</div>;
};

export default UpdatePhotosPlus;
