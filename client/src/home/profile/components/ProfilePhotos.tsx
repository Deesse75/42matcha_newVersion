import { FC } from "react";

type Props = {
  setMatchaNotif: React.Dispatch<React.SetStateAction<string | null>>;
};

const ProfilePhotos: FC<Props> = ({}) => {
  return <div>ProfilePhotos</div>;
};

export default ProfilePhotos;
