import { FC } from "react";

type Props = {
  setMatchaNotif: React.Dispatch<React.SetStateAction<string | null>>;
};

const ProfileBio: FC<Props> = ({setMatchaNotif}) => {
  return <div>ProfileBio</div>;
};

export default ProfileBio;
