import { FC } from 'react';

type Props = {};

const MiniChargement: FC<Props> = ({}) => {
  return (
    <div
      style={{
        color: '#222222',
        fontSize: '10px',
      }}
    >
      {`<...>`}
    </div>
  );
};

export default MiniChargement;
