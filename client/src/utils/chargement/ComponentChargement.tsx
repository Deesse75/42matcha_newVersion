import { FC } from 'react';

type Props = {};

const ComponentChargement: FC<Props> = ({}) => {
  return (
    <div
      style={{
        color: '#222222',
        margin: '5px',
      }}
    >
      En cours ...
    </div>
  );
};

export default ComponentChargement;
